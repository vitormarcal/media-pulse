spring:
  application:
    name: mediapulse-server
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/mediapulse
    username: ${DB_USER:mediapulse}
    password: ${DB_PASS:secret}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
